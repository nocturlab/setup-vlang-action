name: VLang build workflow test

on:
  push:
  pull_request:

jobs:

  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
    - name: Set up VLang version master
      uses: ./
      with:
        v-version: master
      id: v
